# Workers Static Assets headers.
# Bundles under /_expo/static are content-hashed (entry-<hash>.js) — safe to
# cache forever. The HTML shell keeps the default revalidate-always behavior
# so a new deploy is picked up on next navigation.
/_expo/static/*
  Cache-Control: public, max-age=31536000, immutable
